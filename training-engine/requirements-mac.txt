# 训练引擎依赖 - Mac版本
# 移除了Mac不支持的库

# 深度学习框架
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
peft>=0.7.0

# 数据处理
datasets>=2.14.0
pandas>=2.0.0
numpy>=1.24.0

# 优化和加速（Mac兼容）
sentencepiece>=0.1.99
protobuf>=3.20.0

# 监控和日志
tensorboard>=2.14.0

# 工具
tqdm>=4.66.0
scikit-learn>=1.3.0
scipy>=1.11.0

# 注意：
# - bitsandbytes: 不支持Mac，仅Linux+CUDA
# - deepspeed: 不支持Mac，仅Linux+CUDA
# - wandb: 可选，如需要运行: pip install wandb

