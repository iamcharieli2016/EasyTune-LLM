# 训练引擎依赖

# 深度学习框架
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
peft>=0.7.0

# 数据处理
datasets>=2.14.0
pandas>=2.0.0
numpy>=1.24.0

# 优化和加速
# bitsandbytes>=0.41.0  # 量化支持（Mac不支持，仅Linux+CUDA）
# deepspeed>=0.12.0     # 分布式训练（Mac不支持，仅Linux+CUDA）
sentencepiece>=0.1.99  # 某些模型需要
protobuf>=3.20.0

# 监控和日志
tensorboard>=2.14.0
# wandb>=0.15.0  # 可选，如需要可手动安装

# 工具
tqdm>=4.66.0
scikit-learn>=1.3.0
scipy>=1.11.0

