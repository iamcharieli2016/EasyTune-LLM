# 训练引擎依赖 - Linux/CUDA版本
# 包含所有优化库，用于生产环境

# 深度学习框架
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
peft>=0.7.0

# 数据处理
datasets>=2.14.0
pandas>=2.0.0
numpy>=1.24.0

# 优化和加速（仅Linux+CUDA支持）
bitsandbytes>=0.41.0  # 量化支持
deepspeed>=0.12.0     # 分布式训练
sentencepiece>=0.1.99
protobuf>=3.20.0

# 监控和日志
tensorboard>=2.14.0
wandb>=0.15.0

# 工具
tqdm>=4.66.0
scikit-learn>=1.3.0
scipy>=1.11.0

# Flash Attention (可选，需要编译)
# flash-attn>=2.0.0

